#!/bin/bash

# 重启 auditd 服务以加载新的 audisp 插件
echo "Restarting auditd service to load audisp plugin..."
systemctl restart auditd

# 仅在彻底清除时删除数据，重装/升级时 preinst 已处理数据库清理
if [ "$1" = "purge" ]; then
    rm -rf /opt/apps/com.anything
fi